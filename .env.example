# Sao chép thành .env

# Dev (mặc định qua .env.development): dùng proxy local
# VITE_API_URL=/api

# Production / test với Render (cần deploy Backend có route /api/topics)
# VITE_API_URL=https://backend-math-vtfk.onrender.com/api
